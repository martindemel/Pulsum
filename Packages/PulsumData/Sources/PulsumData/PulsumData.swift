// PulsumData static facade removed (P0-03).
// ModelContainer is created at the App layer via DataStack and injected into:
// 1. SwiftUI view hierarchy via .modelContainer(container)
// 2. AgentOrchestrator and agents via their init parameters
